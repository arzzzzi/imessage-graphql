import { Session } from 'next-auth';

interface ConverstionListProps {
  session: Session;
}

const ConverstionList: React.FC<ConverstionListProps> = ({ session }) => {
  return <div></div>;
};

export default ConverstionList;
